#pragma once

#include <string>
#include <unordered_map>

namespace configcat {

// Describes a data source for [FlagOverrides].
class OverrideDataSource {
public:
    // Gets all the overrides defined in the given source.
//    virtual std::unordered_map<std::string> <Map<String, Setting>> getOverrides() = 0;

};

} // namespace configcat
